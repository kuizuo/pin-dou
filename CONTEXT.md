# Context

拼豆图纸生成器把普通图片变成可直接在线下配豆、照图施工的 MARD 图纸。当前只面向使用 5mm MARD 拼豆的新手，处理和作品默认保存在当前设备。

## Language

**作品**：一次可长期保存的创作，包含原图、图片调整、生成设置、当前图纸和版本历史。

**原图**：用户上传的原始文件。按原样保存在设备中，不因预览、转码或去背景而覆盖。

**图案**：从原图中提取并转换后的有豆区域。图案保持原图比例，最长边为用户设置的格数。

**豆板**：承载图案的方形实体板。系统自动选择 52×52、78×78 或 104×104，并把图案居中。

**图纸**：豆板上每一格的最终施工说明，包含空白格或一个有效的 MARD 色号。

**标准施工图**：使用方格、网格、MARD 色号和颜色用量呈现的完整图纸。预览、分享和 PNG 下载使用同一标准，PDF 使用对应的分页施工格式。_Avoid_: 画布截图、当前视图。

**画布**：查看和编辑一张固定豆板的工作空间。拖动画布只改变观察位置，不会移动图案或豆板。_Avoid_: 无限画布、多对象画布。

**图纸工作台**：围绕当前图纸完成查看、整体调整、逐格精修、版本管理和导出的统一空间。_Avoid_: 结果页、独立编辑页。

**编辑锁**：防止图纸格子被意外修改的安全状态。编辑锁开启时仍可拖动和缩放画布；选择编辑工具会自动关闭编辑锁，主动锁定会回到安全的画布拖动状态。每次打开图纸时默认开启。_Avoid_: 画布锁、位置锁。

**一笔**：从开始按下到松开期间，使用同一工具完成的一次连续逐格修改。它作为一个整体保存和撤销。_Avoid_: 每格一次操作。

**整体调整**：会重新生成整张图纸的尺寸、颜色、风格和背景设置。多项调整在用户确认应用后一次生效，并先保留调整前的图纸。_Avoid_: 即时调整、实时生成。

**版本**：作品在某个时刻的完整图纸快照。首次生成、恢复前保护和手动保存会立即产生版本；普通改动只在内容变化且距上次版本满 5 分钟时自动备份，避免连续操作占满设备空间。

**分享图纸**：通过当前设备分享生成后的完整图纸图片。分享内容不包含作品数据、编辑能力或可继续编辑的链接。_Avoid_: 分享作品、协作链接、云端分享。

**空白格**：不放豆的格子。它不是白色。_Avoid_: 透明色、背景白。

**白色豆**：需要放置 MARD T1 的格子，即使视觉上接近页面背景也必须计数。

**颜色上限**：一张图案最多允许出现的 MARD 颜色数，实际颜色数可以更少。

**本机生成**：主体去背景、像素化、配色和统计全部在当前浏览器完成。

**AI 辅助生成**：可以使用应用提供的 Cloudflare AI，也可以填写自己的 Gemini API Key。两种服务都只接收本次裁切后的图片并返回忠实、轮廓清晰的简化稿；Gemini 密钥保存在当前浏览器，不写入作品或备份。浏览器继续负责去背景、配色和图纸生成，普通参数调整不会再次调用 AI。

_Avoid_: AI 图纸、AI 配色、云端作品、自动重复调用 AI。
